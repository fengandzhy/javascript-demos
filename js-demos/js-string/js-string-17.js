/**
 * 1. substr 例如str.substr(0,6);就表示返回从索引0开始往后6位长度的字符
 * 那么str.substr(1,6); 就表示返回从索引1开始往后6位长度的字符
 *
 * */
let str = 'visit Medium!';
let res = str.substr(0,6);
console.log(res);
res = str.substr(1,6);
console.log(res);